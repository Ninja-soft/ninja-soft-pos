import { TienditaView } from "@/components/tiendita/TienditaView";

// Tiendita — storefront del cliente: catálogos precargados de marcas para
// comprar (pago único) y agregar productos a la tienda propia.
export default function TienditaPage() {
  return <TienditaView />;
}
